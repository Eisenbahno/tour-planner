@using MudBlazor

<MudDialog>
    <DialogContent>
        <MudText Typo="Typo.h6">Failed to generate Tour Report.</MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@Close" Color="Color.Primary">OK</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    private void Close()
    {
        MudDialog.Close(DialogResult.Ok(false));
    }

}